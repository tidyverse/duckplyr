# Generated by 02-duckplyr_df-methods.R
#' @export
dplyr_row_slice.duckplyr_df <- function(data, i, ...) {
  # Our implementation
  rel_try(
    # Always fall back to dplyr
    "No relational implementation for dplyr_row_slice()" = TRUE,
    {
      return(out)
    }
  )

  # dplyr forward
  dplyr_row_slice <- dplyr$dplyr_row_slice.data.frame
  out <- dplyr_row_slice(data, i, ...)
  return(out)

  # dplyr implementation
  dplyr_reconstruct(vec_slice(data, i), data)
}

duckplyr_dplyr_row_slice <- function(data, ...) {
  try_fetch(
    data <- as_duckplyr_df(data),
    error = function(e) {
      testthat::skip(conditionMessage(e))
    }
  )
  out <- dplyr_row_slice(data, ...)
  class(out) <- setdiff(class(out), "duckplyr_df")
  out
}
