diff --git b/R/union.R a/R/union.R
--- b/R/union.R
+++ a/R/union.R
@@ -1,13 +1,14 @@
 # Generated by 02-duckplyr_df-methods.R
+#' @rdname union.duckplyr_df
 #' @export
 union.duckplyr_df <- function(x, y, ...) {
   # Our implementation
-  duckplyr_error <- rel_try(NULL,
-    "No relational implementation for {.code union()}" = TRUE,
-    {
-      return(out)
-    }
-  )
+  check_dots_empty()
+
+  # This is difficult to do manually due to order preservation
+  return(distinct(union_all(x, y)))
+
+  duckplyr_error <- NULL
 
   # dplyr forward
   check_funneled(x, duckplyr_error)
