diff --git b/R/inner_join.R a/R/inner_join.R
index aecd66b..91cb6aa 100644
--- b/R/inner_join.R
+++ a/R/inner_join.R
@@ -2,11 +2,16 @@
 #' @importFrom dplyr inner_join
 #' @export
 inner_join.duckplyr_df <- function(x, y, by = NULL, copy = FALSE, suffix = c(".x", ".y"), ..., keep = NULL, na_matches = c("na", "never"), multiple = NULL, unmatched = "drop") {
+  check_dots_empty0(...)
+  error_call <- caller_env()
+
   # Our implementation
   rel_try(
-    "No relational implementation for inner_join()" = TRUE,
+    "Only equi-joins for inner_join()" = inherits(by, "dplyr_join_by") && any(by$condition != "=="),
+    "No implicit cross joins for inner_join()" = is_cross_by(by),
+    "No relational implementation for inner_join(copy = TRUE)" = copy,
     {
-      return(out)
+      return(rel_join_impl(x, y, by, suffix, keep, na_matches, error_call))
     }
   )
 
