diff --git b/R/relocate.R a/R/relocate.R
index 418426e..ffacbbb 100644
--- b/R/relocate.R
+++ a/R/relocate.R
@@ -1,10 +1,23 @@
 # Generated by 02-duckplyr_df-methods.R
 #' @export
 relocate.duckplyr_df <- function(.data, ..., .before = NULL, .after = NULL) {
-  # Our implementation
+  loc <- eval_relocate(
+    expr = expr(c(...)),
+    data = .data,
+    before = enquo(.before),
+    after = enquo(.after),
+    before_arg = ".before",
+    after_arg = ".after"
+  )
+
+  exprs <- exprs_from_loc(.data, loc)
+
+  # Ensure `relocate()` appears in call stack
   rel_try(
-    "No relational implementation for relocate()" = TRUE,
+    "Can't use relational with zero-column result set." = (length(exprs) == 0),
     {
+      rel <- duckdb_rel_from_df(.data)
+      out <- exprs_project(rel, exprs, .data)
       return(out)
     }
   )
